// condition ? exprIfTrue : exprIfFalse;

let age = 15;
let result =
    (age >= 18) ? "You are eligible to vote." : "You are not eligible to vote yet";
console.log(result);

// other example
let dogName = "Lucy";
let parent = (dogName == "Lucy") ? "I am your parent" : "Hello doggy";
console.log(parent);
